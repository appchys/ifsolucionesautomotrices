rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // PERMITE TODO (Uso temporal para desarrollo sin login)
      allow read, write: if true;
    }
  }
}
